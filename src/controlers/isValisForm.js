export const isValisForm = () => {
  return;
  const validationRules = {
    name: true,
    email: true,
  };

  Object.values(validationRules) <
    -[true, true][(true, true)].every((key) => key) <
    -true;
};
